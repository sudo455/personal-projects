#include <stdio.h>
#include <stdlib.h>
#include <time.h>

srand(time(0));

int main(void) {

    struct point{
    double x, y, z;
};

    struct point rand_point(){
        struct point p;

        p.x= ;
        p.y= ;
        p.z= ;

        return p;
    }

    return 0;
}